# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# This workflow will download a prebuilt Ruby version, install dependencies and run tests with Rake
# For more information see: https://github.com/marketplace/actions/setup-ruby-jruby-and-truffleruby

name: Tests

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]

    steps:
    - uses: actions/checkout@v2
    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: 2.7.1
    - name: Install dependencies
      run: bundle install
    - name: Set up cardano-wallet & cardano-node on Shelley testnet
      run: |
        wget https://raw.githubusercontent.com/piotr-iohk/cardano-wallet-rb/master/docker-compose-shelley.yml
        wget https://raw.githubusercontent.com/piotr-iohk/cardano-wallet-rb/master/spec/testnet/testnet-config.json
        wget https://raw.githubusercontent.com/piotr-iohk/cardano-wallet-rb/master/spec/testnet/testnet-byron-genesis.json
        wget https://raw.githubusercontent.com/piotr-iohk/cardano-wallet-rb/master/spec/testnet/testnet-shelley-genesis.json
        wget https://raw.githubusercontent.com/piotr-iohk/cardano-wallet-rb/master/spec/testnet/testnet-topology.json
        NODE_CONFIG_PATH=`pwd` docker-compose -f docker-compose-shelley.yml up --detach
    - name: Run tests
      run: rspec tests
    env:
      NETWORK: testnet
      WALLET: dev-master-shelley
      NODE: 1.23.0
