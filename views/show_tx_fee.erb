<%
# tx_fee
# wallet_id
url_path = request.env['PATH_INFO'].to_s
byron_ep = "/byron-wallets/#{wallet_id}"
shelley_ep = "/wallets/#{wallet_id}"

%>

<div class="list-group">
  <div class="list-group-item">
     Transaction fees  |
       <small>
         <a href="<%= (url_path.include? "byron") ? byron_ep : shelley_ep %>">
           go back to wallet
         </a>
       </small>
  </div>
  <div class="list-group-item">
    <small>
      <b>Estimated min: </b> <%= "#{tx_fee['estimated_min']['quantity']} #{tx_fee['estimated_min']['unit']}" %>
    </small>
    <br/>
    <small>
      <b>Estimated max: </b> <%= "#{tx_fee['estimated_max']['quantity']} #{tx_fee['estimated_max']['unit']}" %>
    </small>
  </div>
</div>
